<div class="card">
    <div class="card-body">
        <h1>{{post.title}}</h1>
        <hr>
        <small>Creation date: {{post.date}}</small><br>
        <hr>
        {{post.content}}
    </div>
</div>